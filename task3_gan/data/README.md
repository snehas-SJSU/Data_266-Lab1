# Task 3 data (not committed)

From the class Kaggle competition **Data** tab, download into this folder:

| File / folder | Purpose |
|---|---|
| `monet_jpg/` | Real Monet paintings (~300) — training + local eval |
| `photo_jpg/` | Real photos (~7038) — training + local eval |
| `real_stats.npz` | Shared Inception reference stats for FID/MiFID (course scoring) |

Also grab from the competition (Data / Code / Overview “Evaluation” link):

- **provided evaluation script** (use this for numbers you self-report)
- **`sample_submission.csv`** if listed — exact column names for `submission.csv`

Kaggle upload for this class competition is **`submission.csv`** (self-reported FID + MiFID), not `images.zip`.
