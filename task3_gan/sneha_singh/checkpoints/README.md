# Checkpoints go here (lab PDF)

Path required by the assignment:

```
task3_gan/sneha_singh/checkpoints/   ← put your .pt files in THIS folder
```

| File | Notes |
|---|---|
| `best.pt` | Full Colab checkpoint (~108 MB) — GitHub blocks >100 MB |
| `best_infer.pt` | Slim generators-only (~43 MB) — optional local/Drive copy |
| `best_smoke.pt` | Smoke run |

`.pt` files are gitignored for size. Folder stays in the repo (via `.gitkeep`).  
Upload weights via **Google Drive** (link in README) or **Git LFS** / GitHub Release if you want them on GitHub.
