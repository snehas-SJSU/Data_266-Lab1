# Checkpoints (Sneha — Part 3)

GitHub rejects files over 100 MB. Full CycleGAN weights are ~108 MB, so they stay off git.

| File | Where |
|---|---|
| `best.pt` | Local / Google Drive (Colab full run, Tesla T4) |
| `best_smoke.pt` | Local only (smoke) |

Train/inference code: `src/part3_cyclegan.ipynb`  
Reload path in notebook: `checkpoints/best.pt` after you place the file here.
